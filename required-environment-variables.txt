# Required Environment Variables for NewsRag API
# Add these to each App Service Configuration → Application settings

# Region-specific (set differently for each region)
DEPLOYMENT_REGION=us        # (us/eu/in for each region)
AZURE_REGION=East US        # (East US/North Europe/Central India)

# OpenAI/Azure OpenAI (same for all regions)
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=your-azure-openai-endpoint
AZURE_OPENAI_API_VERSION=2024-02-01
AZURE_OPENAI_DEPLOYMENT=your-deployment-name
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=your-embedding-deployment
AZURE_OPENAI_EMBEDDING_MODEL=text-embedding-3-large
EMBEDDING_DIMENSION=3072

# Qdrant Vector Database (same for all regions)
QDRANT_URL=your-qdrant-url
QDRANT_API_KEY=your-qdrant-api-key
QDRANT_COLLECTION_NAME=news_articles

# Application Settings (same for all regions)
CACHE_TTL=3600
DEFAULT_ARTICLE_LIMIT=50
MAX_ARTICLE_CONTENT_CHARS=10000
MAX_CHUNK_SIZE=1000
SUMMARY_CACHE_SIZE=1000
SUMMARY_CACHE_TTL=3600

# Optional: Monitoring (same for all regions)
LANGFUSE_HOST=your-langfuse-host
LANGFUSE_PUBLIC_KEY=your-public-key
LANGFUSE_SECRET_KEY=your-secret-key