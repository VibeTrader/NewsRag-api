# ============================================
# Terraform .gitignore
# Prevents sensitive and temporary files from being committed
# ============================================

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*
*tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# ============================================
# Environment and Secret Files
# ============================================

# Environment files that might contain secrets
.env
.env.local
.env.*.local

# Any files containing secrets or sensitive data
*secret*
*password*
*key*
*token*

# ============================================
# Backup and Temporary Files  
# ============================================

# Backup files
*.backup
*.bak
*_backup*
backup_*

# Temporary files
*.tmp
*.temp
*~

# Editor and IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ============================================
# PowerShell and Windows specific
# ============================================

# PowerShell transcript files
PowerShell_transcript.*

# Windows shortcuts
*.lnk

# ============================================
# Custom for NewsRag Project
# ============================================

# Terraform lock files (optional - some teams commit these)
# Uncomment next line if you don't want to commit lock files
# .terraform.lock.hcl

# Generated documentation
docs/generated/

# Test outputs
test_output/
*.test

# Local configuration overrides
local.tf
local.tfvars

# Scripts with embedded secrets (if any)
*_with_secrets.*
deploy_with_*

# Any extraction or migration files
extract_*
migration_*
current_app_settings.txt

# Terraform backend configuration (if it contains sensitive data)
# backend.tf

# ============================================
# Application specific (in case any app files end up here)
# ============================================

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/

# Node modules (if any JS tooling is used)
node_modules/

# Logs
*.log

# ============================================
# IMPORTANT: What TO commit
# ============================================

# ✅ DO commit these files:
# - *.tf (main Terraform configuration files)
# - *.md (documentation) 
# - .gitignore (this file)
# - environments/*.tfvars.example (example configurations without secrets)
# - README files
# - modules/ (your custom modules)

# ❌ DON'T commit these:
# - *.tfvars (contains potentially sensitive configuration)
# - *.tfstate (contains sensitive state information)
# - .terraform/ (local cache and plugins)
# - Any files with actual API keys, passwords, or secrets
